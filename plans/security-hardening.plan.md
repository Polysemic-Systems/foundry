# Security Hardening

1. [x] Implement the necessary refactoring in cgroup_policy.rs to house policy/probe classification logic, modify runner.rs to expose only the required runtime probe primitive, and establish comprehensive regression tests to verify that malicious workload outputs cannot trigger resource downgrades. - files: crates/foundry-cli/src/cgroup_policy.rs, crates/foundry-cli/src/runner.rs - run: cargo test -p foundry-cli cgroup_policy - id: implement-the-necessary-refactoring-in
2. [x] Allow propose approval to create a new repository-local plan while preserving leased path confinement, and add regression tests that accept a missing plan beneath the workspace but reject path escapes and symlinked ancestors. - files: crates/foundry-cli/src/lease.rs, crates/foundry-cli/src/commands/propose.rs - run: cargo test -p foundry-cli propose_new_plan - id: propose-can-create-new-plan
3. [x] Add a regression that RepositoryMutation::require_path has no production expect/panic path, then replace the current expect with propagated context while preserving confinement behavior. - files: crates/foundry-cli/src/lease.rs - run: cargo test -p foundry-cli require_path_has_no_production_expect - id: require-path-no-production-expect
4. [x] Update the existing TDD vertical-slice fake Podman runners so Foundry's trusted `true` cgroup probe does not advance their simulated green/red/green workload phases. - files: crates/foundry-cli/tests/vertical_slice.rs - run: cargo test -p foundry-cli --test vertical_slice - id: tdd-fake-runner-probe-ordering
